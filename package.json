{
  "name": "docker-best-practices-app",
  "version": "1.0.0",
  "description": "A production-ready Node.js app demonstrating Docker best practices",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "echo 'Build step for TypeScript apps would go here'",
    "test": "jest --coverage",
    "health-check": "curl -f http://localhost:3000/health || exit 1"
  },
  "keywords": [
    "docker",
    "best-practices",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "winston": "^3.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
